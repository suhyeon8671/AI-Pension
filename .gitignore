__pycache__/
*.pyc
.env
.venv/

# 빌드 산출물 중 vector_store 자체(임베딩 프로바이더 pickle 등)만 제외한다 -
# GitHub 단일 파일 100MB 제한에 걸리고 재생성 시간도 부담 없다.
# product_master.json 등 구조화 데이터 JSON들은 서버가 클론 직후 바로
# 응답해야 하는 배포 환경(재추출 없이 즉시 서비스)을 위해 커밋한다.
vector_store/
structured_store.db
